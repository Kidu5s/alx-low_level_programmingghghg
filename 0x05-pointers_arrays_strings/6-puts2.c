#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints out a string
 * @str: string
 *
 * Return: void
 */

void puts2(char *str)
{
	int i;
	
	i = strlen(s);
	if (i
	for (i = 0; i >= str % 2; i++);
	 {
		 puts(s);
	 }
}
