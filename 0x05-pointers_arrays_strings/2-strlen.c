#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: the string
 *
 * Return: strlen
 */

int _strlen(char *s)
{
	int length;

	for (length = 0; length <= s[length]; length++)
	{
	
	}
	return (length);
}
