#include <stdio.h>

/**
 * _putchar - writes the character x to stdout
 * @x: the character to print
 *
 * Return: always 0
 */
int _putchar(void)
{
	return (0);
}

/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	char a, b, c, d, e, f, g, h;

	a = '_';
	b = 'p';
	c = 'u';
	d = 't';
	e = 'c';
	f = 'h';
	g = 'a';
	h = 'r';
	putchar(a);
	putchar(b);
	putchar(c);
	putchar(d);
	putchar(e);
	putchar(f);
	putchar(g);
	putchar(h);
	putchar('\n');
	return (0);
}
